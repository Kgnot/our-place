services:
#  db:
#    build:
#      context: ./db
#      dockerfile: Dockerfile
#    container_name: our_place_db
#    environment:
#      POSTGRES_DB: ${DB_NAME:-ourplace}
#      POSTGRES_USER: ${DB_USER:-postgres}
#      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"
#    healthcheck:
#      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME:-ourplace}" ]
#      interval: 5s
#      timeout: 5s
#      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: our_place
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev}

      DB_URL: jdbc:postgresql://db:5432/${DB_NAME:-ourplace}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}

      JWT_SECRET: ${JWT_SECRET}

      R2_ENDPOINT: ${R2_ENDPOINT}
      R2_ACCESS_KEY: ${R2_ACCESS_KEY}
      R2_SECRET_KEY: ${R2_SECRET_KEY}
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID}
      R2_BUCKET: ${R2_BUCKET:-our-place-media}

      NEW_RELIC_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY}
      NEW_RELIC_APP_NAME: ${NEW_RELIC_APP_NAME}
      NEW_RELIC_LOG_LEVEL: ${NEW_RELIC_LOG_LEVEL:-info}
      NEWRELIC_ACCOUNT_ID: ${NEWRELIC_ACCOUNT_ID}

    restart: unless-stopped
#
#volumes:
#  pgdata:
